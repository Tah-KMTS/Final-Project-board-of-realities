// House rule: VEHICLE_TYPES was removed when game/VehicleActor.js was
// rewritten from a procedural class to an atlas-sprite one (see that file's
// header) - dropped from this re-export too since it no longer exists.
export { VehicleActor } from '../../game/VehicleActor'
